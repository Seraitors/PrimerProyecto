package EstructurasDeControl;

import java.util.Scanner;

public class Ejercicio5 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        //programa que solicite 10 numeros por teclado
        //vamos a usar dos variables auxiliares :mas y min
        //max vamos a ir guardando temporalamente el numero mayor qu etegamos
        //en min ""     ""    ""     ""     ""      "" menor que tengamos

        System.out.println("numero");;
        int n1 = sc.nextInt();
        //al pedir el primer numero eso es el mayor y el menor que tenemos de momento asique las asignamos
        int max =n1;
        int min= n1;
        System.out.println("numero");;
        int n2 = sc.nextInt();
        if (n2> max){// si n2 es mayor que el que tenemos lo guardamos hay
            max =n2;
        }
        if (n2 <min){// si n2 es menor que el que teniamos lo guardamos en min

        }
        System.out.println("numero");;
        int n3 = sc.nextInt();
        if (n3> max){// si n2 es mayor que el que tenemos lo guardamos hay
            max =n2;
        }
        if (n3 <min){// si n2 es menor que el que teniamos lo guardamos en min

        }
        System.out.println("numero");;
        int n4 = sc.nextInt();
        if (n4> max){// si n2 es mayor que el que tenemos lo guardamos hay
            max =n2;
        }
        if (n4 <min){// si n2 es menor que el que teniamos lo guardamos en min

        }
        System.out.println("numero");;
        int n5 = sc.nextInt();
        if (n5> max){// si n2 es mayor que el que tenemos lo guardamos hay
            max =n5;
        }
        if (n5 <min){// si n2 es menor que el que teniamos lo guardamos en min

        }
        System.out.println("numero");;
        int n6 = sc.nextInt();
        if (n6> max){// si n2 es mayor que el que tenemos lo guardamos hay
            max =n6;
        }
        if (n6 <min){// si n2 es menor que el que teniamos lo guardamos en min

        }
        System.out.println("numero");;
        int n7 = sc.nextInt();
        if (n7> max){// si n2 es mayor que el que tenemos lo guardamos hay
            max =n7;
        }
        if (n7 <min){// si n2 es menor que el que teniamos lo guardamos en min

        }
        System.out.println("numero");;

        int n8 = sc.nextInt();
        if (n8> max){// si n2 es mayor que el que tenemos lo guardamos hay
            max =n8;
        }
        if (n8 <min){// si n2 es menor que el que teniamos lo guardamos en min

        }
        System.out.println("numero");;
        int n9 = sc.nextInt();
        if (n9> max){// si n2 es mayor que el que tenemos lo guardamos hay
            max =n9;
        }
        if (n9 <min){// si n2 es menor que el que teniamos lo guardamos en min

        }
        System.out.println("numero");;
        int n10 = sc.nextInt();
        if (n10> max){// si n2 es mayor que el que tenemos lo guardamos hay
            max =n10;
        }
        if (n10 <min){// si n2 es menor que el que teniamos lo guardamos en min

        }
        System.out.println( " el numero mayor es " + max);
        System.out.println("el numero menso es  " + min);
    }
}
